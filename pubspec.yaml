name: clarity
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^8.0.2
  flutter_svg: ^2.2.4
  device_preview: ^1.3.1
  fl_chart: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1

# 👇 YE YAHA HOGA (IMPORTANT)
flutter_icons:
  android: true
  ios: true
  image_path: "assets/app logo.png"

flutter:
  uses-material-design: true

  assets:
    - assets/